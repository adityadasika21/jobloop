{# =============================================================================
   Follow-up on an application that has gone silent — FIXED FORMAT.

   Only the middle paragraph varies. Everything else is verbatim.

   The paragraph must give them a reason to reply, not a reminder that they
   didn't. Note what the format deliberately does NOT say: how long it has
   been. Counting the days out loud is what makes a follow-up read as a
   complaint, and this one asks for a status while adding something new.
   ============================================================================= #}
Hi {{ contact_name }}, hope you're doing well!

I applied for the {{ role }} role at {{ company }}{% if applied_on %} on {{ applied_on }}{% endif %} and wanted to follow up directly. {{ pitch }}

Is there an update on where the process stands? Happy to share anything else that would help.

Thanks!
Aditya
