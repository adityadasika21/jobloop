# Weakness ledger — opened by interview debriefs and screening gaps,
# closed only by evidence of recovery in a later real interview.
# Managed by `jt learn` and `jt debrief`; safe to read, avoid hand-editing.
weaknesses: []
