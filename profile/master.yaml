# =============================================================================
# MASTER PROFILE — the single source of truth for every claim about Aditya.
#
# RULE: Nothing may appear on a generated resume that is not traceable to an
# evidence unit in this file. `jt verify` enforces this mechanically.
#
# An evidence unit is ONE atomic true fact. Tailoring selects, orders, and
# rephrases these units in the JD's vocabulary — it never invents.
#
# Reconstructed 2026-08-18 from ~/Downloads/Aditya_Resume.pdf (1 page, letter,
# Jake-Ryan-style LaTeX). Reconciled 2026-08-19 against Aditya's authoritative
# source, reference/resume-source.tex — where the two disagreed, his won.
# =============================================================================

identity:
  name: Aditya Dasika
  headline: "AI/ML Engineer · Agentic & Multi-Agent Systems · RAG · LLM Eval, Observability & Guardrails"
  email: akhil.dasika47@gmail.com
  phone: "+91-96184-33030"
  links:
    portfolio: https://projectindex.online
    github: https://github.com/adityadasika21
    linkedin: https://www.linkedin.com/in/adityadasika21
  # anchor | bare. Anchor reads better to a human; a bare URL is the only form
  # that survives PDF text extraction. Aditya chose anchor on 2026-08-19 with
  # that tradeoff on the table. `jt ats` reports which form it recovered.
  link_style: anchor
  link_labels:
    portfolio: Portfolio
    github: GitHub
    linkedin: LinkedIn
  location: Hyderabad, Telangana, India
  # Used by `jt screen` to flag seniority mismatches honestly rather than hide them.
  experience_start: 2022-05
  degree_field: Mechanical Engineering

roles:
  - id: phenom-ai-llm-eng-ii
    title: AI/LLM Engineer II
    company: Phenom People
    location: Hyderabad, Telangana
    start: 2025-04
    end: present
    domain: [hr-tech, hr, talent-intelligence, recruitment, hiring,
             talent-acquisition, enterprise-saas]

  - id: blueleaves-fullstack
    title: Full Stack Engineer
    company: Blueleaves Farms
    location: Hyderabad, Telangana
    start: 2022-05
    end: 2025-02
    domain: [retail, agritech, e-commerce, operations, supply-chain]

education:
  - id: edu-aliet
    institution: Andhra Loyola Institute of Engineering and Technology
    location: Vijayawada, Andhra Pradesh
    degree: Bachelor of Technology in Mechanical Engineering
    start: 2018-08
    end: 2022-05

# -----------------------------------------------------------------------------
# EVIDENCE UNITS
#
# claim      — the canonical true statement, as it appears on the master resume
# skills     — normalized tokens; a tailored bullet may NOT introduce a skill
#              token that is absent from the units it cites (enforced by `jt verify`)
# metrics    — hard numbers, quoted verbatim; never round or inflate these
# keywords   — JD-side vocabulary this unit legitimately answers to. These are
#              synonyms for what was ACTUALLY done, not aspirational adjacencies.
# strength   — core | strong | supporting. Drives selection under space pressure.
# probe      — what an interviewer will ask if this bullet earns a screen pass.
#              If you can't answer the probe, the bullet is too aggressive.
# emphasize  — the load-bearing terms this unit may render in bold. Aditya's own
#              resume bolds the system name, the framework, and the one number
#              worth arguing about; bolding every number bolds nothing. A term
#              is only honoured where it appears verbatim in the bullet, and
#              `jt verify` rejects any term the cited evidence doesn't support.
# -----------------------------------------------------------------------------

evidence:

  # ---- Phenom People : agentic systems -------------------------------------
  - id: ev-phenom-chatbotcx-architecture
    role: phenom-ai-llm-eng-ii
    claim: >-
      Architected chatbotCXAgent, a production multi-agent system on LangGraph
      (12 orchestrated nodes) spanning intent recognition, planning,
      dialogue-state management, and tool routing — with Graph RAG retrieval over
      enterprise knowledge and document repositories via FAISS vector search and a
      Neo4j graph for multi-hop, policy-aware grounding.
    skills: [langgraph, multi-agent-systems, agent-orchestration, intent-recognition,
             planning, dialogue-state-management, tool-routing, graph-rag, rag,
             faiss, neo4j, vector-search, production-systems]
    metrics: {orchestrated_nodes: "12"}
    keywords: [agentic ai, multi-agent, agent orchestration, conversational ai,
               chatbot, state machine, retrieval augmented generation, knowledge graph,
               semantic search, grounding, llm application, production ml,
               agent workflows, workflows, rag pipelines, orchestration]
    strength: core
    probe: "Why 12 nodes? How does dialogue state survive a tool failure mid-turn?
            Why a graph alongside vectors instead of vectors alone?"
    emphasize: [chatbotCXAgent, LangGraph, Graph RAG]

  - id: ev-phenom-toolcalling-guardrails
    role: phenom-ai-llm-eng-ii
    claim: >-
      Engineered tool-calling agents that execute governed actions — database
      queries and structured extraction from unstructured documents — under
      XGrammar constrained decoding and schema-validated JSON guardrails, raising
      tool-routing accuracy to 91.9% and eliminating malformed tool calls in live
      traffic.
    skills: [tool-calling, function-calling, constrained-decoding, xgrammar,
             json-schema, schema-validation, guardrails, structured-extraction,
             sql, agent-reliability]
    metrics: {tool_routing_accuracy: "91.9%", malformed_tool_calls: "eliminated in live traffic"}
    keywords: [function calling, tool use, structured output, json mode,
               constrained generation, guardrails, agent reliability, data extraction,
               document understanding, text-to-sql]
    strength: core
    probe: "How did you measure routing accuracy? What was the baseline?
            Why XGrammar over plain JSON mode or retry-on-parse-failure?"
    emphasize: ["XGrammar constrained decoding", XGrammar, 91.9%]

  # Rewritten 2026-08-19 from Aditya's current source. The earlier version of
  # this unit was reconstructed from the older PDF and described the *config
  # and policy plane* — registry, feature groups, resolve API. His current
  # bullet leads with the detection and decisioning layer: an LLM orchestration
  # engine that investigates and produces reviewer-facing assessments. That is
  # a materially stronger and much more agentic claim, and it is the one a
  # screen should see. The config-plane work is still true, so it survives
  # below as its own unit rather than being deleted — master.yaml is the
  # evidence store, not the resume, and a tailored bullet may cite either.
  - id: ev-phenom-fraud-detection-engine
    role: phenom-ai-llm-eng-ii
    claim: >-
      Building the detection and decisioning layer of an enterprise
      hiring-integrity / fraud-risk platform — an LLM orchestration engine that
      investigates flagged candidate activity, correlates evidence across
      signals, and produces reviewer-facing assessments with supporting
      rationale — backed by a policy-driven rules engine (score / condition →
      action), per-tenant risk configuration, and audited review workflows
      across the signal lifecycle.
    skills: [llm-orchestration, agent-orchestration, multi-agent-systems,
             evidence-correlation, decision-support, explainability,
             fraud-detection, risk-scoring, anomaly-investigation, policy-engine,
             rules-engine, multi-tenancy, audit-logging, system-design]
    metrics: {}
    keywords: [agentic ai, llm orchestration, llm agents, investigation,
               decision engine, rules engine, policy engine, risk scoring,
               fraud detection, trust and safety, anomaly detection,
               human-in-the-loop, reviewer workflow, explainable ai, rationale,
               multi-tenant, audit trail, enterprise saas, hr tech, platform engineering]
    strength: core
    emphasize: ["hiring-integrity / fraud-risk platform", "LLM orchestration engine",
                "policy-driven rules engine"]
    probe: "What does the orchestration actually decide — does the LLM score, or
            does it assemble evidence for a rules engine that scores? How do you
            keep a reviewer-facing rationale faithful to the signals it cites?
            What is the false-positive cost of accusing a real candidate?"

  - id: ev-phenom-fraud-policy-plane
    role: phenom-ai-llm-eng-ii
    claim: >-
      Designed the configuration and policy plane for an enterprise
      hiring-integrity / fraud-risk platform: a global fraud-signal registry with
      feature groups, tenant-scoped overrides, and applicable-context gating,
      exposed through a resolve API consumed by the reviewer console and the live
      detection pipeline — including a role-gated signal-lifecycle state machine,
      versioned definitions that keep downstream consumers stable across config
      changes, and CEL-evaluated decision policies (score / condition → action).
    skills: [system-design, policy-engine, multi-tenancy, feature-flags, cel,
             api-design, versioning, rbac, state-machine, fraud-detection,
             risk-scoring, backend-architecture]
    metrics: {}
    keywords: [platform engineering, policy engine, rules engine, multi-tenant,
               configuration management, api design, trust and safety, fraud detection,
               risk platform, backward compatibility, access control, enterprise saas]
    strength: strong
    # Not on the master resume: his current page carries one fraud bullet, the
    # detection engine above. This unit stays available to tailoring — a
    # platform/backend JD should be able to lead with it.
    on_master: false
    emphasize: ["hiring-integrity / fraud-risk platform", "resolve API"]
    probe: "How do versioned definitions avoid breaking live consumers?
            Why CEL over a DSL you own? How are tenant overrides resolved and cached?"

  - id: ev-phenom-eval-framework
    role: phenom-ai-llm-eng-ii
    claim: >-
      Built an LLM evaluation framework (pass@k, LLM-as-judge rubric) gating every
      release across all 12 nodes, instrumented with LiteLLM + Langfuse for
      per-node cost, latency, and full-trace auditability — continuously monitoring
      agent behaviour and curbing hallucinations.
    skills: [llm-evaluation, llm-as-judge, pass-at-k, eval-harness, langfuse,
             litellm, observability, tracing, cost-monitoring, latency-monitoring,
             regression-testing, hallucination-mitigation, ci-gating]
    metrics: {nodes_covered: "12"}
    keywords: [llm evaluation, evals, benchmarking, model monitoring, observability,
               tracing, guardrails, quality gates, ci/cd for ml, hallucination,
               responsible ai, llmops, cost optimization, evaluation harness,
               eval harnesses, release gate, machine learning]
    strength: core
    probe: "What does your judge rubric look like and how did you validate the judge
            itself? What happens on a release that regresses one node out of twelve?"
    emphasize: ["LLM evaluation framework", "LiteLLM + Langfuse"]

  - id: ev-phenom-finetune-serving
    role: phenom-ai-llm-eng-ii
    claim: >-
      Fine-tuned and served Qwen models via QLoRA (Unsloth, NF4) on vLLM /
      AWS g5.xlarge (A10G), sustaining 10.98 RPS at 98.6% success under load at
      roughly half the inference cost of hosted GPT-4.1.
    skills: [fine-tuning, qlora, peft, unsloth, quantization, nf4, vllm, qwen,
             model-serving, gpu-inference, aws, load-testing, cost-optimization,
             throughput-optimization]
    metrics: {throughput: "10.98 RPS", success_rate: "98.6%",
              cost: "~50% of hosted GPT-4.1 inference cost", hardware: "AWS g5.xlarge (A10G)"}
    keywords: [fine-tuning, lora, qlora, peft, model serving, inference optimization,
               quantization, open source llm, open-source models, self-hosted llm, gpu,
               throughput, latency, cost reduction, mlops, model deployment, deploying,
               serving stack, machine learning]
    strength: core
    probe: "What batch size and context length at 10.98 RPS? What was the 1.4% failure
            mode? How did you construct the QLoRA training set?"
    emphasize: [QLoRA, vLLM]

  # Aditya's source folds this into the QLoRA bullet ("; separately fine-tuned a
  # multi-task ModernBERT..."). Kept split here because tailoring wants the
  # granularity — a PyTorch/NLU role should be able to lead with it. To rejoin
  # under space pressure, write ONE bullet citing both unit ids; provenance is a
  # list and `jt verify` checks the merged text against the union.
  - id: ev-phenom-modernbert-nlu
    role: phenom-ai-llm-eng-ii
    claim: >-
      Fine-tuned a multi-task ModernBERT (intent + NER) in PyTorch, raising NLU
      accuracy from 87% to 97%.
    skills: [pytorch, modernbert, bert, transformers, multi-task-learning,
             intent-classification, named-entity-recognition, nlu, fine-tuning,
             model-training]
    metrics: {nlu_accuracy_before: "87%", nlu_accuracy_after: "97%"}
    keywords: [nlp, natural language understanding, intent classification, ner,
               entity extraction, transformers, bert, encoder models, multi-task,
               model training, classification]
    strength: strong
    probe: "Why multi-task over two separate heads? Where did the 10 points come from —
            data, architecture, or labels?"
    emphasize: [ModernBERT]
    # Confirmed by Aditya 2026-08-20. His notes elsewhere say "mmBERT" — that
    # is loose shorthand, not the shipped model. ModernBERT is correct and is
    # what his own resume source says. Do not "fix" this.

  # ---- Blueleaves Farms : full stack ---------------------------------------
  - id: ev-blueleaves-analytics
    role: blueleaves-fullstack
    claim: >-
      Built data-driven analytics on large-scale transactional and stock-movement
      data — demand forecasting, sales-trend, and inventory models that surfaced
      discrepancies and improved procurement/replenishment decisions across
      5+ locations and 10,000+ monthly transactions.
    skills: [data-analytics, demand-forecasting, time-series, inventory-modeling,
             sql, anomaly-detection, business-intelligence, python]
    metrics: {locations: "5+", monthly_transactions: "10,000+"}
    keywords: [data analysis, forecasting, predictive modeling, time series,
               supply chain, inventory optimization, analytics, reporting,
               anomaly detection, business impact]
    strength: supporting
    probe: "What forecasting method, and what did you measure it against?"

  - id: ev-blueleaves-fullstack-delivery
    role: blueleaves-fullstack
    claim: >-
      Owned end-to-end delivery of production systems (POS, CRM, inventory,
      procurement, e-commerce) on Node.js, Firebase Cloud Functions, and REST APIs
      — with role-based access control and CI/CD — integrating multiple internal
      systems and cutting manual operational errors by roughly 90%.
    skills: [nodejs, firebase, cloud-functions, rest-api, rbac, ci-cd,
             system-integration, full-stack, serverless, product-ownership,
             e-commerce, crm]
    metrics: {manual_error_reduction: "~90%"}
    keywords: [full stack, backend, api development, serverless, microservices,
               system integration, access control, ci/cd, end-to-end ownership,
               production systems, saas]
    strength: supporting
    probe: "What did 'owned end-to-end' include — did you handle on-call and incidents?"

  # ---- Projects -------------------------------------------------------------
  - id: ev-proj-northstar
    kind: project
    name: NorthStar — Motorcycle Navigation Companion App
    stack: [Android, BLE, Maps SDK]
    claim: >-
      Built and shipped a companion app for the Royal Enfield Tripper Dash,
      delivering turn-by-turn navigation to the bike's connected instrument cluster
      and growing an organic user base without paid distribution.
    skills: [android, kotlin, bluetooth-low-energy, maps-sdk, mobile-development,
             reverse-engineering, embedded-integration, product-shipping]
    metrics: {distribution: "organic, no paid acquisition"}
    keywords: [mobile, android, embedded, iot, bluetooth, navigation, consumer product,
               shipped product, side project, ownership]
    strength: strong
    probe: "How did you talk to a cluster you had no spec for?"

  - id: ev-proj-northstar-traction
    kind: project
    parent: ev-proj-northstar
    claim: >-
      Traction drew Royal Enfield's attention directly, leading to an on-site
      visit at their HQ and a continuing relationship — they have since
      approached him about a role and about collaborating on other
      motorcycle-accessory work.
    skills: [product-shipping, stakeholder-communication, industry-partnership]
    metrics: {}
    keywords: [impact, recognition, traction, industry validation, initiative,
               partnership, inbound interest, business development]
    strength: strong
    emphasize: [Royal Enfield]
    # Confirmed by Aditya 2026-08-20: the visit was not a dead end. What ended
    # was the app — the repo is archived and he was asked to stop distributing
    # it. The relationship is what continued, which is the stronger fact and
    # the one the claim states. Do not let the claim imply the app is live.
    probe: "What did they actually want from the visit, and where did the app
            itself end up? Be ready to say plainly that you were asked to stop
            distributing it and why that did not end the relationship."

  - id: ev-proj-indexnotes
    kind: project
    name: IndexNotes — AI Knowledge Graph for Scientific Papers
    stack: [Neo4j, Ollama, PyMuPDF, Python, Docker]
    claim: >-
      Built an automated Graph RAG pipeline ingesting research papers
      (PDF → local-LLM extraction → structured JSON → Neo4j), creating 40+ concepts
      and 17+ typed relationships, with quantized local models via Ollama for
      entity/relationship extraction.
    skills: [graph-rag, neo4j, ollama, local-llm, quantization, pymupdf,
             document-ingestion, structured-extraction, knowledge-graph, python,
             docker, etl]
    metrics: {concepts: "40+", typed_relationships: "17+"}
    keywords: [knowledge graph, graph rag, retrieval, document processing, pdf parsing,
               information extraction, entity extraction, relationship extraction,
               local llm, on-device, pipeline, etl]
    strength: strong
    probe: "How do you keep a local quantized model's extractions consistent enough
            to build a graph on?"

  - id: ev-proj-indexnotes-schema
    kind: project
    parent: ev-proj-indexnotes
    claim: >-
      Designed a Neo4j schema enabling multi-hop graph traversal and
      cross-document retrieval across scientific domains.
    skills: [neo4j, graph-schema-design, cypher, multi-hop-retrieval, data-modeling]
    metrics: {}
    keywords: [graph database, schema design, data modeling, multi-hop reasoning,
               cross-document retrieval, cypher]
    strength: supporting
    probe: "What does a multi-hop query actually look like here?"

  - id: ev-phenom-frm-service
    role: phenom-ai-llm-eng-ii
    claim: Built the fraud-risk configuration service greenfield in FastAPI on
      MongoDB — signal registry, feature groups, audience and scope definitions,
      and tenant-scoped overrides — exposed through an internal resolve endpoint
      that Temporal workflows call at workflow start to decide which fraud signals
      apply to a candidate and job.
    skills: [fastapi, python, mongodb, rest-api, api-design, backend-architecture,
      multi-tenancy, configuration-management, temporal, workflow-orchestration,
      greenfield-development, service-design]
    metrics: {}
    keywords: [backend engineer, fastapi, python, rest api, api design, microservices,
      mongodb, nosql, configuration service, control plane, multi-tenant, tenant
        configuration, workflow orchestration, temporal, distributed systems,
      platform engineering, greenfield, service ownership, trust and safety, fraud
        detection, hr tech]
    strength: core
    emphasize: [FastAPI, MongoDB, Temporal]
    probe: Why an internal resolve call at workflow start rather than the workflow
      reading config itself? What happens to a running workflow when the config
      changes underneath it?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'Two months (mid-June to 2026-08-20) building Fraud Risk Management
      end to end at Phenom: greenfield FastAPI/Mongo config-plane service with
      a signal registry, feature groups, audiences/scopes and tenant overrides,
      exposed as /internal/resolve and consumed by Temporal workflows at workflow
      start; merged that resolve into chatbot-interview-intelligence''s Temporal
      workflows and removed Policy Engine gating so FRM decides what runs; built
      the React admin UI (feature store, audiences, tenants, case/action centre)
      over the chatbot-authentication service with Keycloak; tenant provisioning
      with dcRegion filtering; Docker/Helm staging deploys and ClickHouse log
      debugging for production support tickets. He owns FRM backend, orchestration
      and rules-engine; other people own interview-intelligence and its workflows.'

  - id: ev-phenom-frm-decides-what-runs
    role: phenom-ai-llm-eng-ii
    claim: Merged fraud-risk resolve into the interview-intelligence Temporal
      workflows and removed the Policy Engine gating that had decided which checks
      to run, making the configuration service the single source of truth for
      fraud enablement and replacing hardcoded per-tenant flags with audience
      rules resolved per candidate.
    skills: [temporal, workflow-orchestration, system-integration, refactoring,
      backend-architecture, feature-flags, policy-engine, cross-repo-migration,
      single-source-of-truth]
    metrics: {}
    keywords: [workflow orchestration, temporal, integration, migration, decoupling,
      feature flags, configuration driven, policy engine, rules engine, refactoring,
      legacy replacement, distributed systems, backend, platform engineering]
    strength: core
    emphasize: [single source of truth, Temporal, Policy Engine]
    probe: What broke when you took the Policy Engine out of that path, and how
      did you roll it out without stopping live interviews? How do you know a
      tenant's behaviour did not change silently?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'Two months (mid-June to 2026-08-20) building Fraud Risk Management
      end to end at Phenom: greenfield FastAPI/Mongo config-plane service with
      a signal registry, feature groups, audiences/scopes and tenant overrides,
      exposed as /internal/resolve and consumed by Temporal workflows at workflow
      start; merged that resolve into chatbot-interview-intelligence''s Temporal
      workflows and removed Policy Engine gating so FRM decides what runs; built
      the React admin UI (feature store, audiences, tenants, case/action centre)
      over the chatbot-authentication service with Keycloak; tenant provisioning
      with dcRegion filtering; Docker/Helm staging deploys and ClickHouse log
      debugging for production support tickets. He owns FRM backend, orchestration
      and rules-engine; other people own interview-intelligence and its workflows.'

  - id: ev-phenom-frm-admin-ui
    role: phenom-ai-llm-eng-ii
    claim: Built the fraud administration front end in React — feature store,
      audience rules, tenant provisioning, and a case and action centre for reviewers
      — authenticated through the chatbot authentication service with Keycloak,
      and built to an existing design system and Figma specs.
    skills: [react, javascript, frontend, ui-development, design-systems, keycloak,
      oauth, authentication, sso, csp, full-stack, figma]
    metrics: {}
    keywords: [react, frontend, full stack, ui, admin console, internal tools,
      design system, figma, authentication, sso, keycloak, oauth, access control,
      reviewer workflow, case management, dashboards]
    strength: strong
    emphasize: [React, Keycloak]
    probe: How does the front end get a token, and what happens when it expires
      mid-session? What did the Content-Security-Policy and iframe work actually
      fix?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'Two months (mid-June to 2026-08-20) building Fraud Risk Management
      end to end at Phenom: greenfield FastAPI/Mongo config-plane service with
      a signal registry, feature groups, audiences/scopes and tenant overrides,
      exposed as /internal/resolve and consumed by Temporal workflows at workflow
      start; merged that resolve into chatbot-interview-intelligence''s Temporal
      workflows and removed Policy Engine gating so FRM decides what runs; built
      the React admin UI (feature store, audiences, tenants, case/action centre)
      over the chatbot-authentication service with Keycloak; tenant provisioning
      with dcRegion filtering; Docker/Helm staging deploys and ClickHouse log
      debugging for production support tickets. He owns FRM backend, orchestration
      and rules-engine; other people own interview-intelligence and its workflows.'

  - id: ev-phenom-frm-tenancy-ops
    role: phenom-ai-llm-eng-ii
    claim: Owned tenant provisioning and delivery for the platform — provision
      and unprovision APIs with region-scoped tenant listing, containerised deployment
      via Docker and Helm across staging environments, and production support
      debugging by querying service logs in ClickHouse to resolve customer tickets.
    skills: [multi-tenancy, provisioning, docker, helm, helm-charts,
      deployment, environment-configuration, clickhouse, log-analysis, production-support,
      debugging, sre]
    metrics: {}
    keywords: [multi-tenant, provisioning, tenant onboarding, docker, helm, deployment,
      staging, environment configuration, devops, sre, operations, production operations, operational support,
      observability, log analysis,
      clickhouse, production support, incident, debugging, on-call]
    strength: strong
    emphasize: [Docker, Helm, ClickHouse]
    probe: What does provisioning a tenant actually create, and how do you unprovision
      one safely? Walk me through a ticket you closed from the ClickHouse logs.
    on_master: false
    added_at: '2026-08-20'
    source_note: 'Two months (mid-June to 2026-08-20) building Fraud Risk Management
      end to end at Phenom: greenfield FastAPI/Mongo config-plane service with
      a signal registry, feature groups, audiences/scopes and tenant overrides,
      exposed as /internal/resolve and consumed by Temporal workflows at workflow
      start; merged that resolve into chatbot-interview-intelligence''s Temporal
      workflows and removed Policy Engine gating so FRM decides what runs; built
      the React admin UI (feature store, audiences, tenants, case/action centre)
      over the chatbot-authentication service with Keycloak; tenant provisioning
      with dcRegion filtering; Docker/Helm staging deploys and ClickHouse log
      debugging for production support tickets. He owns FRM backend, orchestration
      and rules-engine; other people own interview-intelligence and its workflows.'

  - id: ev-phenom-analytics-service
    role: phenom-ai-llm-eng-ii
    claim: Built conversation analytics APIs covering both live-agent and WhatsApp
      channels, raised the service's test coverage from 63% to over 90% while
      clearing static-analysis code smells, and migrated the service to FastAPI.
    skills: [python, fastapi, rest-api, api-design, analytics, testing, test-coverage,
      pytest, static-analysis, code-quality, refactoring, service-migration]
    metrics: {test_coverage_before: 63%, test_coverage_after: over 90%}
    keywords: [backend, python, fastapi, rest api, api development, analytics,
      reporting, conversation analytics, unit testing, test coverage, code quality,
      static analysis, sonarqube, refactoring, migration, technical debt, maintainability]
    strength: strong
    emphasize: [63% to over 90%, FastAPI]
    probe: What was untested at 63%, and did raising coverage actually catch anything?
      Why migrate to FastAPI rather than leave it?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'Mar-Aug 2026 at Phenom, alongside the agent and fraud work:
      chatbot-analytics conversation APIs for agent and WhatsApp channels, test
      coverage raised from 63% to over 90% and Sonar code smells cleared, plus
      a FastAPI migration; chatbot-hub WhatsApp business messaging - templates,
      the 24-hour session window, from-address lookup API, questionnaire id from
      job; the chatbot-llm-inference-engine deployed by Helm with custom chat
      templates, reasoning-model flags (DeepSeek R1), OpenTelemetry instrumentation
      and a metrics dashboard; micro-frontend widget surfaces - summary API, integrity-check
      modal, live-signals aggregation, typeahead locations, video player in a
      modal; NLU work with Locust load tests, PII skip/classify and retraining
      on failed intents; Kafka event-handler fixes.'

  - id: ev-phenom-whatsapp-channel
    role: phenom-ai-llm-eng-ii
    claim: Delivered WhatsApp as a conversational channel — message templates,
      the 24-hour customer-service window the API enforces, and a from-address
      lookup API — so the assistant could reach candidates outside the web widget.
    skills: [whatsapp-business-api, messaging, third-party-integration, rest-api,
      api-design, nodejs, python, conversational-channels]
    metrics: {session_window: "24-hour customer-service window"}
    keywords: [whatsapp, messaging, business messaging, notifications, third party
        integration, api integration, conversational ai, omnichannel, customer
        engagement, templates, webhooks]
    strength: supporting
    emphasize: [WhatsApp]
    probe: What actually happens when the 24-hour window closes mid-conversation,
      and how did you handle template approval?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'Mar-Aug 2026 at Phenom, alongside the agent and fraud work:
      chatbot-analytics conversation APIs for agent and WhatsApp channels, test
      coverage raised from 63% to over 90% and Sonar code smells cleared, plus
      a FastAPI migration; chatbot-hub WhatsApp business messaging - templates,
      the 24-hour session window, from-address lookup API, questionnaire id from
      job; the chatbot-llm-inference-engine deployed by Helm with custom chat
      templates, reasoning-model flags (DeepSeek R1), OpenTelemetry instrumentation
      and a metrics dashboard; micro-frontend widget surfaces - summary API, integrity-check
      modal, live-signals aggregation, typeahead locations, video player in a
      modal; NLU work with Locust load tests, PII skip/classify and retraining
      on failed intents; Kafka event-handler fixes.'

  - id: ev-phenom-inference-platform
    role: phenom-ai-llm-eng-ii
    claim: Ran the self-hosted LLM inference service as a deployable platform
      — Helm-packaged releases, custom chat templates per model, reasoning-model
      flags, and OpenTelemetry instrumentation feeding a metrics dashboard so
      per-model latency and throughput were visible in production.
    skills: [llm-serving, vllm, helm, helm-charts, docker, deployment, opentelemetry,
      observability, metrics, monitoring, chat-templates, model-configuration,
      mlops, platform-engineering]
    metrics: {}
    keywords: [llm serving, self-hosted llm, model deployment, inference, mlops,
      llmops, helm, helm charts, release packaging, observability, opentelemetry, tracing,
      metrics, monitoring, dashboards, platform engineering, production ml, reasoning
        models]
    strength: strong
    emphasize: [Helm, OpenTelemetry]
    probe: What do the OpenTelemetry spans actually cover on an inference request,
      and what did the dashboard change about how you operate it?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'Mar-Aug 2026 at Phenom, alongside the agent and fraud work:
      chatbot-analytics conversation APIs for agent and WhatsApp channels, test
      coverage raised from 63% to over 90% and Sonar code smells cleared, plus
      a FastAPI migration; chatbot-hub WhatsApp business messaging - templates,
      the 24-hour session window, from-address lookup API, questionnaire id from
      job; the chatbot-llm-inference-engine deployed by Helm with custom chat
      templates, reasoning-model flags (DeepSeek R1), OpenTelemetry instrumentation
      and a metrics dashboard; micro-frontend widget surfaces - summary API, integrity-check
      modal, live-signals aggregation, typeahead locations, video player in a
      modal; NLU work with Locust load tests, PII skip/classify and retraining
      on failed intents; Kafka event-handler fixes.'

  - id: ev-phenom-widget-frontend
    role: phenom-ai-llm-eng-ii
    claim: Built candidate-facing surfaces in the micro-frontend widget — a conversation
      summary API and view, an integrity-check modal, live-signal aggregation,
      location typeahead, and in-modal video playback.
    skills: [react, micro-frontend, frontend, javascript, typescript, ui-development,
      rest-api, component-design, video-playback]
    metrics: {}
    keywords: [frontend, react, micro frontend, mfe, widget, ui, components, user
        experience, typeahead, autocomplete, modal, video, embed, candidate experience,
      full stack]
    strength: supporting
    emphasize: [micro-frontend]
    probe: Why a micro-frontend rather than a component in the host app, and how
      do you keep the widget's bundle from bloating the page it embeds into?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'Mar-Aug 2026 at Phenom, alongside the agent and fraud work:
      chatbot-analytics conversation APIs for agent and WhatsApp channels, test
      coverage raised from 63% to over 90% and Sonar code smells cleared, plus
      a FastAPI migration; chatbot-hub WhatsApp business messaging - templates,
      the 24-hour session window, from-address lookup API, questionnaire id from
      job; the chatbot-llm-inference-engine deployed by Helm with custom chat
      templates, reasoning-model flags (DeepSeek R1), OpenTelemetry instrumentation
      and a metrics dashboard; micro-frontend widget surfaces - summary API, integrity-check
      modal, live-signals aggregation, typeahead locations, video player in a
      modal; NLU work with Locust load tests, PII skip/classify and retraining
      on failed intents; Kafka event-handler fixes.'

  - id: ev-proj-northstar-protocol
    kind: project
    parent: ev-proj-northstar
    claim: Reverse-engineered the dash's undocumented wire protocol — an RSA-1024
      plus AES-256 handshake over UDP with auto-discovery of any dash advertising
      itself on the local network, and an H.264 over RTP video plane on a separate
      port.
    skills: [reverse-engineering, network-protocols, udp, rtp, h264, video-streaming,
      cryptography, rsa, aes, packet-analysis, embedded-integration, android]
    metrics: {}
    keywords: [reverse engineering, protocol, binary protocol, networking, udp,
      rtp, streaming, video, h264, encryption, cryptography, handshake, service
        discovery, embedded, iot, device integration, low level]
    strength: strong
    emphasize: [RSA-1024, AES-256, H.264]
    probe: How did you recover the handshake without a spec — traffic capture,
      firmware, or both? What happens if a second phone tries to pair while you
      are connected?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'From Aditya''s own project summary, 2026-08-20. NorthStar internals:
      RSA-1024 + AES-256 handshake over UDP:2000, auto-discovery of any RE_* dash
      on WiFi, H.264/RTP video over UDP:5000; maps rendered off-screen with MapLibre
      and OpenFreeMap tiles, routed via OSRM, so the phone screen could stay off,
      destination arriving through Android''s share sheet. Qwen3.5-2B QLoRA in
      production on vLLM: temp 0.1, tool_choice required, thinking disabled via
      a custom Jinja2 chat template, --enforce-eager because CUDA graphs break
      on the hybrid Mamba+attention architecture; golden baseline P50 1.0s, P95
      1.4s, zero failures over 5,510 requests; test coverage 7% to 78%. Blueleaves:
      sole full-stack engineer, CRM and internal tools as React + Vite web apps.
      PINNs from scratch: heat equation ~3% error, Burgers via two-phase Adam
      then L-BFGS matching Raissi et al., Schrodinger complex-valued with periodic
      boundaries and Latin-hypercube sampling. Corpus: Android payment tracker,
      SMS and email ingestion, installed and working on his phone.'

  - id: ev-proj-northstar-offscreen-maps
    kind: project
    parent: ev-proj-northstar
    claim: Rendered navigation off-screen with MapLibre and OpenFreeMap tiles
      and routed through OSRM, streaming the result to the bike's cluster so the
      phone's screen could stay off entirely — where the official app mirrors
      the phone display — with destinations arriving through Android's share sheet.
    skills: [android, kotlin, maplibre, osrm, offscreen-rendering, opengl, geospatial,
      routing, mobile-development, battery-optimization, system-integration]
    metrics: {}
    keywords: [android, mobile, maps, mapping, geospatial, routing, navigation,
      rendering, graphics, offscreen rendering, performance, battery life, share
        sheet, intents, product thinking]
    strength: strong
    emphasize: [MapLibre, OSRM, off-screen]
    probe: What is the actual frame path from an off-screen surface to the cluster,
      and what did rendering off-screen cost you in power or latency versus mirroring?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'From Aditya''s own project summary, 2026-08-20. NorthStar internals:
      RSA-1024 + AES-256 handshake over UDP:2000, auto-discovery of any RE_* dash
      on WiFi, H.264/RTP video over UDP:5000; maps rendered off-screen with MapLibre
      and OpenFreeMap tiles, routed via OSRM, so the phone screen could stay off,
      destination arriving through Android''s share sheet. Qwen3.5-2B QLoRA in
      production on vLLM: temp 0.1, tool_choice required, thinking disabled via
      a custom Jinja2 chat template, --enforce-eager because CUDA graphs break
      on the hybrid Mamba+attention architecture; golden baseline P50 1.0s, P95
      1.4s, zero failures over 5,510 requests; test coverage 7% to 78%. Blueleaves:
      sole full-stack engineer, CRM and internal tools as React + Vite web apps.
      PINNs from scratch: heat equation ~3% error, Burgers via two-phase Adam
      then L-BFGS matching Raissi et al., Schrodinger complex-valued with periodic
      boundaries and Latin-hypercube sampling. Corpus: Android payment tracker,
      SMS and email ingestion, installed and working on his phone.'

  - id: ev-phenom-qwen-production-hardening
    role: phenom-ai-llm-eng-ii
    claim: Hardened the fine-tuned Qwen deployment for production serving — a
      custom Jinja2 chat template disabling the model's thinking mode, required
      tool-choice and temperature pinned at 0.1 for deterministic tool calls,
      and eager execution forced because CUDA graphs break on the model's hybrid
      Mamba-attention architecture — then held it to a golden baseline of 1.0s
      median and 1.4s P95 latency with zero failures across 5,510 requests, taking
      the service's test coverage from 7% to 78%.
    skills: [vllm, model-serving, inference-optimization, latency-optimization,
      jinja2, chat-templates, tool-calling, cuda, gpu-inference, load-testing,
      benchmarking, regression-testing, test-coverage, production-readiness]
    metrics: {p50_latency: 1.0s, p95_latency: 1.4s, requests_without_failure: '5,510',
      temperature: '0.1', test_coverage_before: 7%, test_coverage_after: 78%}
    keywords: [llm serving, inference, vllm, latency, p95, benchmarking, load
        testing, golden baseline, regression testing, tool calling, deterministic
        output, gpu, cuda, production readiness, slo, performance tuning, test
        coverage, mlops]
    strength: core
    emphasize: [1.4s, '5,510', 7% to 78%]
    probe: Why does disabling thinking mode matter for tool calls, and what did
      --enforce-eager cost you in throughput? What does the golden baseline gate
      — is it wired into CI?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'From Aditya''s own project summary, 2026-08-20. NorthStar internals:
      RSA-1024 + AES-256 handshake over UDP:2000, auto-discovery of any RE_* dash
      on WiFi, H.264/RTP video over UDP:5000; maps rendered off-screen with MapLibre
      and OpenFreeMap tiles, routed via OSRM, so the phone screen could stay off,
      destination arriving through Android''s share sheet. Qwen3.5-2B QLoRA in
      production on vLLM: temp 0.1, tool_choice required, thinking disabled via
      a custom Jinja2 chat template, --enforce-eager because CUDA graphs break
      on the hybrid Mamba+attention architecture; golden baseline P50 1.0s, P95
      1.4s, zero failures over 5,510 requests; test coverage 7% to 78%. Blueleaves:
      sole full-stack engineer, CRM and internal tools as React + Vite web apps.
      PINNs from scratch: heat equation ~3% error, Burgers via two-phase Adam
      then L-BFGS matching Raissi et al., Schrodinger complex-valued with periodic
      boundaries and Latin-hypercube sampling. Corpus: Android payment tracker,
      SMS and email ingestion, installed and working on his phone.'

  - id: ev-blueleaves-frontend
    role: blueleaves-fullstack
    claim: Sole engineer on the whole stack — built the CRM and the other internal
      tools as React and Vite web applications, owning the interface as well as
      the services behind it.
    skills: [react, vite, javascript, frontend, full-stack, ui-development, crm,
      internal-tools, sole-ownership, spa]
    metrics: {}
    keywords: [react, vite, frontend, full stack, single page application, internal
        tools, crm, dashboards, sole developer, ownership, end to end, web application]
    strength: strong
    emphasize: [React, Vite, Sole engineer]
    probe: What did you get wrong building a CRM alone that a second engineer
      would have caught? How did you keep the UI maintainable without review?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'From Aditya''s own project summary, 2026-08-20. NorthStar internals:
      RSA-1024 + AES-256 handshake over UDP:2000, auto-discovery of any RE_* dash
      on WiFi, H.264/RTP video over UDP:5000; maps rendered off-screen with MapLibre
      and OpenFreeMap tiles, routed via OSRM, so the phone screen could stay off,
      destination arriving through Android''s share sheet. Qwen3.5-2B QLoRA in
      production on vLLM: temp 0.1, tool_choice required, thinking disabled via
      a custom Jinja2 chat template, --enforce-eager because CUDA graphs break
      on the hybrid Mamba+attention architecture; golden baseline P50 1.0s, P95
      1.4s, zero failures over 5,510 requests; test coverage 7% to 78%. Blueleaves:
      sole full-stack engineer, CRM and internal tools as React + Vite web apps.
      PINNs from scratch: heat equation ~3% error, Burgers via two-phase Adam
      then L-BFGS matching Raissi et al., Schrodinger complex-valued with periodic
      boundaries and Latin-hypercube sampling. Corpus: Android payment tracker,
      SMS and email ingestion, installed and working on his phone.'

  - id: ev-proj-pinns
    kind: project
    name: Physics-Informed Neural Networks from scratch
    stack: [PyTorch, NumPy, L-BFGS]
    claim: Implemented physics-informed neural networks from scratch across three
      PDEs — the heat equation to roughly 3% error, Burgers' equation using two-phase
      Adam then L-BFGS optimisation reproducing the published benchmark, and the
      Schrodinger equation with complex-valued outputs, periodic boundary conditions
      and Latin-hypercube sampling.
    skills: [pytorch, physics-informed-neural-networks, scientific-computing,
      numerical-methods, partial-differential-equations, optimization, lbfgs,
      automatic-differentiation, numpy, research-implementation]
    metrics: {heat_equation_error: ~3%}
    keywords: [scientific machine learning, physics informed neural networks,
      pinns, numerical methods, differential equations, simulation, optimization,
      pytorch, autograd, research, applied mathematics, computational physics,
      modelling]
    strength: strong
    emphasize: [PyTorch, L-BFGS]
    probe: Why does a naive PINN fail on Burgers without the two-phase optimiser?
      What did you use as ground truth for the 3% figure?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'From Aditya''s own project summary, 2026-08-20. NorthStar internals:
      RSA-1024 + AES-256 handshake over UDP:2000, auto-discovery of any RE_* dash
      on WiFi, H.264/RTP video over UDP:5000; maps rendered off-screen with MapLibre
      and OpenFreeMap tiles, routed via OSRM, so the phone screen could stay off,
      destination arriving through Android''s share sheet. Qwen3.5-2B QLoRA in
      production on vLLM: temp 0.1, tool_choice required, thinking disabled via
      a custom Jinja2 chat template, --enforce-eager because CUDA graphs break
      on the hybrid Mamba+attention architecture; golden baseline P50 1.0s, P95
      1.4s, zero failures over 5,510 requests; test coverage 7% to 78%. Blueleaves:
      sole full-stack engineer, CRM and internal tools as React + Vite web apps.
      PINNs from scratch: heat equation ~3% error, Burgers via two-phase Adam
      then L-BFGS matching Raissi et al., Schrodinger complex-valued with periodic
      boundaries and Latin-hypercube sampling. Corpus: Android payment tracker,
      SMS and email ingestion, installed and working on his phone.'

  - id: ev-proj-corpus
    kind: project
    name: Corpus — personal finance tracker
    stack: [Android, Kotlin, SQLite]
    claim: Built and shipped an Android personal-finance tracker that ingests
      payments automatically by parsing SMS and email rather than asking for manual
      entry, tracks instalment schedules, and runs on his own phone daily.
    skills: [android, kotlin, mobile-development, text-parsing, information-extraction,
      local-storage, product-design, shipping]
    metrics: {}
    keywords: [android, mobile, kotlin, personal finance, fintech, parsing, text
        extraction, automation, on-device, side project, shipped]
    strength: supporting
    emphasize: [Android]
    probe: How reliable is the SMS parsing across banks, and what happens when
      a bank changes its message format?
    on_master: false
    added_at: '2026-08-20'
    source_note: 'From Aditya''s own project summary, 2026-08-20. NorthStar internals:
      RSA-1024 + AES-256 handshake over UDP:2000, auto-discovery of any RE_* dash
      on WiFi, H.264/RTP video over UDP:5000; maps rendered off-screen with MapLibre
      and OpenFreeMap tiles, routed via OSRM, so the phone screen could stay off,
      destination arriving through Android''s share sheet. Qwen3.5-2B QLoRA in
      production on vLLM: temp 0.1, tool_choice required, thinking disabled via
      a custom Jinja2 chat template, --enforce-eager because CUDA graphs break
      on the hybrid Mamba+attention architecture; golden baseline P50 1.0s, P95
      1.4s, zero failures over 5,510 requests; test coverage 7% to 78%. Blueleaves:
      sole full-stack engineer, CRM and internal tools as React + Vite web apps.
      PINNs from scratch: heat equation ~3% error, Burgers via two-phase Adam
      then L-BFGS matching Raissi et al., Schrodinger complex-valued with periodic
      boundaries and Latin-hypercube sampling. Corpus: Android payment tracker,
      SMS and email ingestion, installed and working on his phone.'

  # <<< jt evidence add
  # New units are spliced in immediately above this marker. It exists so
  # master.yaml can be appended to without a YAML round-trip, which would
  # strip every comment in this file — including the rules at the top that
  # tell the next writer what an evidence unit is allowed to be.

# -----------------------------------------------------------------------------
# SKILLS — the master inventory, grouped as on the current resume.
# `jt tailor` reorders groups and prunes within them to match the JD.
# It may NEVER add an entry that is not listed here.
# -----------------------------------------------------------------------------

skill_groups:
  - name: Languages
    items: [Python, JavaScript, TypeScript, SQL, Bash]
  - name: GenAI & Agentic
    items: ["Agentic / Multi-Agent Systems", LangGraph, LangChain,
            "Tool/Function Calling", "Prompt Engineering", "Model Routing",
            "Context-Window Management", "RAG & Graph RAG",
            "LLM Fine-Tuning (QLoRA, SFT)"]
  - name: "Eval, Observability & Guardrails"
    items: ["LLM Evaluation (LLM-as-judge, pass@k)", OpenTelemetry,
            "Constrained Decoding (XGrammar)", "Schema Validation", Langfuse,
            LiteLLM, "Model Monitoring", "Hallucination Mitigation",
            "Role-Based Access Control"]
  - name: ML & Serving
    items: [PyTorch, "Transformers (BERT/ModernBERT)", "HuggingFace PEFT", Unsloth,
            "Physics-Informed Neural Networks",
            vLLM, "Embeddings / Vector Search (FAISS)", Neo4j, Ollama,
            "Production MLOps"]
  - name: Backend & Infra
    items: [FastAPI, Node.js, Express.js, "REST APIs", Microservices, Docker,
            Helm, Temporal, Kafka, "AWS (EC2/S3/SageMaker)", "GitHub Actions",
            CI/CD, Linux]
  - name: Frontend & Auth
    items: [React, Vite, "Micro-Frontends", Keycloak, "OAuth / SSO", "Design Systems",
            "Role-Based Access Control"]
  - name: Databases
    items: [PostgreSQL, Neo4j, MongoDB, ClickHouse, Redis, Firebase]

# -----------------------------------------------------------------------------
# KNOWN GAPS — things JDs ask for that are NOT in the evidence above.
# `jt screen` reads this so a gap is reported honestly instead of quietly
# becoming a fabricated bullet. Move an entry into `evidence` only when it
# becomes genuinely true.
# -----------------------------------------------------------------------------

known_gaps:
  - topic: dspy
    status: aware-not-used
    note: "Seen in JDs alongside LangChain. Evaluated, not shipped."
  - topic: llamaindex
    status: aware-not-used
    note: "Comparable to LangChain work but not used in production."
  - topic: gcp-vertex-ai
    status: adjacent
    note: "AWS-based (EC2/S3/SageMaker/g5). Vertex is transferable but unused."
  - topic: kubernetes
    status: adjacent
    note: "Docker in production; orchestration was not owned."
  - topic: csharp-dotnet
    status: structural
    note: "Python/JS/TS. Blackbaud's AI Platform Engineer hard-requires
           'Proficient in C# .NET' — flag any JD that names a .NET stack."
  - topic: vendor-agent-platforms
    status: aware-not-used
    note: "Microsoft Copilot Studio, Azure AI Foundry, Agentforce, IBM watsonx
           Orchestrate. Agent orchestration built on LangGraph/Temporal instead;
           the concepts transfer, the vendor consoles were never used."
  - topic: cs-degree
    status: structural
    note: "B.Tech Mechanical. Flag when a JD hard-requires CS/related degree."
  - topic: years-of-experience
    status: structural
    note: "~4.3 yrs total (since 2022-05), ~1.4 yrs explicitly AI/LLM titled.
           Flag JDs requiring 6+ yrs so the application is a deliberate stretch."
